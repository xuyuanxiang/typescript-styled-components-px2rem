import styled from 'styled-components';

const Styled1 = styled.button`
  height: 100px;
  line-height: 100px;
  font-size: 18px;
`;

const Styled2 = styled.button<{ height: number }>`
  height: ${props => props.height}px;
  line-height: 100px;
  font-size: 18px;
`;

const Styled3 = styled.button<{ height: number; fontSize: number }>(
  props => `
  height: ${props.height}px;
  line-height: 100px;
  font-size: ${props.fontSize}px;
`,
);

const mixins = `
  height: 100px;
  line-height: 100px;
  font-size: 18px;
`;

const Styled4 = styled.button`
  ${mixins}
`;
